import { TransformOptions } from '@babel/core';
import { Loader, Plugin } from 'esbuild';
import { FilterPattern, Plugin as Plugin$1 } from 'vite';

/**
 * Original: https://github.com/nativew/esbuild-plugin-babel
 * Copied and customized, because there was a problem with `type: "module"` in `package.json`
 */
interface ESBuildPluginBabelOptions {
    config?: TransformOptions;
    filter?: RegExp;
    customFilter: (id: unknown) => boolean;
    namespace?: string;
    loader?: Loader | ((path: string) => Loader);
}
declare const esbuildPluginBabel: (options: ESBuildPluginBabelOptions) => Plugin;

interface BabelPluginOptions {
    apply?: 'serve' | 'build';
    babelConfig?: TransformOptions;
    filter?: RegExp;
    include?: FilterPattern;
    exclude?: FilterPattern;
    loader?: Loader | ((path: string) => Loader);
}
declare const babelPlugin: ({ babelConfig, filter, include, exclude, apply, loader }?: BabelPluginOptions) => Plugin$1;

export { type BabelPluginOptions, type ESBuildPluginBabelOptions, babelPlugin as default, esbuildPluginBabel };
